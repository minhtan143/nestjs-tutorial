name: nestjs-tutorial

services:
  dev-db:
    image: postgres
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: nest
    networks:
      - nestjs-tutorial-network

networks:
  nestjs-tutorial-network:
